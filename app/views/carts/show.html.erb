<% if notice %>
  <p id="notice"><%= notice %></p>
<% end %>

<h2>Your Pragmatic Cart</h2>
<ul>
  <% @cart.line_items.each do |item| %>
    <li>
      <%= "Qty: #{item.quantity}" %>
      <%= item.product.title %>
    </li>
  <% end %>  
</ul>
